# lodash: fixed via package.json overrides (lodash >=4.17.23); lock file resolves to 4.17.23
# Trivy may still report from dependency declaration; ignore until scanner uses resolved version
CVE-2025-13465

# OpenSSL in base image (Alpine): libcrypto3/libssl3 3.5.4-r0 → 3.5.5-r0
# Base image (digital-step-flow-base-node) must be rebuilt with apk upgrade to pick up fix
CVE-2025-15467
CVE-2025-69419
