# Prometheus config for local development
# Scrapes backend metrics (and optional targets)
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'backend'
    static_configs:
      - targets: ['backend:8082']
    metrics_path: /metrics
