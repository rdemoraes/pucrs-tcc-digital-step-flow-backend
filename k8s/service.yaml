apiVersion: v1
kind: Service
metadata:
  name: digital-step-flow-backend
  namespace: digital-step-flow
  labels:
    app: digital-step-flow-backend
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: digital-step-flow-backend

