# KICS configuration - exclude queries that are false positives for this project
# https://docs.kics.io/latest/configuration-file/

# HIGH: POSTGRES_PASSWORD / GRAFANA_ADMIN_PASSWORD in docker-compose are env var
# references for local dev (.env), not hardcoded secrets
exclude-queries:
  - a88baa34-e2ad-44ea-ad6f-8cac87bc7c71
